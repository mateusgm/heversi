
*************************** the game ******************************

# player
{IA, human}

# move
((x, y), p) 

# board
[[p]]

               make                 change
  a player   --------->  a move   ---------->  the board


while (!isFinished)
   m = get move (p);
   b = make move (b); 
   if (p == p1) p = p2; else p = p1;
endwhile;
